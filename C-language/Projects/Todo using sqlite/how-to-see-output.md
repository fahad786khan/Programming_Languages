# Todo using C and Sqlite3

1. First you need a "3.49.1" atleast sqlite3 then install sqlite-devel

2. make a todo.db and run sqlite3 todo.db < input.sql

# Run

gcc main.c -o app -lsqlite3

./app