#include<stdio.h>
#include<math.h>

/*increment operators are ++i and i++
decrement operators are --i and i--
*/

int main(){
    int i=2;
    // printf("%d \n",i++); // use, then increase
    // printf("%d \n",++i); //increase, then use
    
   // printf("%d \n",i--);//use, then decrease
      printf("%d \n",--i);//decrese,then use
      printf("%d \n",i);
    return 0;
}