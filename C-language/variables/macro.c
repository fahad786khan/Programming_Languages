#include <stdio.h>
#define POWER(x) ((x) * (x))

// macro and a symbolic constant is that a macro can accept an argument and typically contains code

void main()
{
    printf("%u\n", POWER(4)); // 16
}