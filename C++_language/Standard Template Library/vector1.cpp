#include <bits/stdc++.h>

using namespace std;
int main()
{
    vector<int> numbers;
    int value;
    for (int i = 0; i < 5; i++)
    {
        cout << "Enter the value : ";
        cin >> value;
        numbers.push_back(value);
    }
    return 0;
}