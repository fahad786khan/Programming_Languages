#include<iostream>
//cin is used to read the value
using namespace std;

int main(){
    cout << "Enter values for x and y: ";
    double x, y;
    cin >> x >> y;
    cout << x + y << endl;

    return 0;
}